# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "sdp_lesson"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-30ac84d8-3e9b.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-30ac84d8-3e9b.cloud.databricks.com
      # We always use /Users/martinguitar000@gmail.com for all resources to make sure we only have a single copy.
      root_path: /Users/martinguitar000@gmail.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as martinguitar000@gmail.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: martinguitar000@gmail.com
